# Local development only. Replace the placeholder before starting PostgreSQL.
POSTGRES_USER=ai_studio
POSTGRES_PASSWORD=replace_with_local_password
POSTGRES_DB=ai_studio
POSTGRES_PORT=5432
# Keep credentials and port consistent; URL-encode special characters.
DATABASE_URL=postgresql://ai_studio:replace_with_local_password@localhost:5432/ai_studio?schema=public
AI_PROVIDER=mock
